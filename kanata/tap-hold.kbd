(defalias
  controls-switch (layer-switch controls)
  controls-hold (layer-while-held controls)
)

(defalias
  spl (tap-hold 0 $hold-time spc lsft)
  bsc (tap-hold $tap-time $hold-time bspc @controls-hold)
  rly (tap-hold $tap-time $hold-time ret lmet)
  cdr (tap-hold 0 $hold-time sldr lctl)
)
