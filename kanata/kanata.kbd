(include config.kbd)
(include chordsv2.kbd)

(defalias
  sctl (tap-hold $hold-time $hold-time spc lctl)
)

(deflayer colmak
  _    _    _    _    _    _    _    _    _    _    _    _    _    _
  _    q    w    f    p    b    j    l    u    y    ;    _    _    _
  _    a    r    s    t    g    m    n    e    i    o    _    _
  _    z    x    c    d    v    k    h    ,    .    /    _
  _    _    _              @sctl          _    _    _
)
