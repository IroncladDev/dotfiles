(defoverrides
  (lmet n) (left)
  (lmet e) (down)
  (lmet i) (up)
  (lmet o) (right)

  (lsft bspc) (lalt bspc)

  (lmet q) (prev)
  (lmet w) (pp)
  (lmet f) (next)
  (lmet p) (vold)
  (lmet b) (volu)
)
